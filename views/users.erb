<h1>All Users</h1>
  <% @users.each_with_index do |user, index| %>
    <p><%= index + 1 %>. <a href="/users/<%= user.id %>"><%= user.name %></a> </p>
    <p><%= user.color %></p>
  <% end %>

  <a href="/">Home</a>

